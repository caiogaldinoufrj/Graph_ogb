# Core ML libs
torch>=1.8.0
torchvision>=0.9.0

# PyTorch Geometric ecosystem (these often require matching wheels for your CUDA/PyTorch)
# Install the following with the official PyG wheel index to match your torch+cuda:
# Example (replace <torch+cuda> with the right tag, e.g. torch-1.12.1+cu113):
# pip install torch-scatter torch-sparse torch-cluster torch-spline-conv torch-geometric -f https://data.pyg.org/whl/torch-<torch+cuda>.html
torch-scatter
torch-sparse
torch-cluster
torch-spline-conv
torch-geometric

# Libraries used in scripts and utils
numpy>=1.16.0
pandas>=0.24.0
tqdm
ogb>=1.3.2

# Embedding / NLP
sentence-transformers

# Logging / tensorboard
tensorboard
tensorboardX

# Optional helper libs
pickle5

# Notes:
# - Some packages above (torch-scatter, torch-sparse, torch-cluster, torch-spline-conv, torch-geometric)
#   must be installed with wheels compatible with your torch and CUDA versions. Follow:
#   https://pytorch-geometric.readthedocs.io/en/latest/notes/installation.html
# - If you use CPU-only, pick the appropriate "torch+cpu" wheel tag from PyG's index.
